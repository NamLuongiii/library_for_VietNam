import { show } from '@/app/help/base'

export default async function Header() {
  return (
    <header className="h-14 border-b sticky top-0 z-50 flex justify-start items-center gap-6 px-4 bg-primary"></header>
  )
}
